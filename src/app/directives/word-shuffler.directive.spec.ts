import { WordShufflerDirective } from './word-shuffler.directive';

describe('WordShufflerDirective', () => {
  it('should create an instance', () => {
    const directive = new WordShufflerDirective();
    expect(directive).toBeTruthy();
  });
});
